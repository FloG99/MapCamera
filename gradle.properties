version=1.0.0

# The MapGUI API this compiles against, from Maven Central. A checkout of MapGUI next to this one is
# substituted for it by settings.gradle.kts where there is one, so the two can be worked on together.
mapguiVersion=1.1.0
paperVersion=26.2.build.84-stable

org.gradle.caching=true
org.gradle.parallel=true
